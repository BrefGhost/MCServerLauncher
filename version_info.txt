# Windows version resource embedded in the exe.
#
# It does not stop SmartScreen - only a code-signing certificate or download
# reputation does that - but it is what makes the file's Properties dialog show
# a real name and version instead of blanks, and heuristic scanners weigh a
# blank resource against a file.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 4, 0),
    prodvers=(1, 0, 4, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo([
      StringTable(
        "040904B0",
        [StringStruct("CompanyName", "BrefGhost"),
         StringStruct("FileDescription",
                      "MC Server Launcher - run a Minecraft modpack server"),
         StringStruct("FileVersion", "1.0.4"),
         StringStruct("InternalName", "MCServerLauncher"),
         StringStruct("LegalCopyright", "MIT License"),
         StringStruct("OriginalFilename", "MC Server Launcher.exe"),
         StringStruct("ProductName", "MC Server Launcher"),
         StringStruct("ProductVersion", "1.0.4")])
    ]),
    VarFileInfo([VarStruct("Translation", [1033, 1200])])
  ]
)
